# Global gitignore. Loaded automatically by git from
# $XDG_CONFIG_HOME/git/ignore (default location, no core.excludesFile needed).

# Legacy per-repo agent-time log (the toggl-time opencode plugin used to
# write JSONL here before heartbeats moved to a central SQLite DB). Kept
# as cheap insurance against committing stragglers; safe to remove once
# you're confident no `.toggl-time` files remain on disk.
/.toggl-time
