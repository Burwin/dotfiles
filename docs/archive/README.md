# archive

Decision-history snapshots of plans that have shipped. The implementation
they describe lives in the source tree; this folder exists so the "why"
isn't lost. Files inside may reference paths (e.g. `TESTS.md`) that no
longer exist — that's expected.
